#include <stdio.h>

int main() {
int arr[100],n,I;
scanf(“%d”,&n);
for(i=0;i<n;i++)
{ 
   scanf(“%d”,&arr[i]);
}

    int sum = 0;
    for (i = 0; i < size; i += 2) {
        sum += arr[i];
    }

    printf("Sum of alternate elements: %d\n", sum);

    return 0;
}
